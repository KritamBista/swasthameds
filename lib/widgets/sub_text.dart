import 'package:flutter/material.dart';

class SubText extends StatelessWidget {
  const SubText({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text("view all >",style: TextStyle(color:Color(0xFF48BFEA)),);
  }
}